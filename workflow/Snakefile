# The highest level logic of the workflow is defined here
# Here **and only here** should be defined which other .smk files are included
# Here define the rule `all` gathering all the outputs of the workflow
# Here can be defined also other highlevel snakemake stuff like `localrules`, `min_version` or `report`
#
# Please follow the best practices:
# https://snakemake.readthedocs.io/en/stable/snakefiles/best_practices.html,
