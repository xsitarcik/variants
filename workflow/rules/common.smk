# here should be all workflow-related logic such as parameter handling, config parsing and so on.
# logic representing rules not written in .smk but in Python or R should be placed in scripts/
# here should be also the validation of config
